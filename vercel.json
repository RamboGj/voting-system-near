{
  "version": 2,
  "builds": [
    {
      "src": "frontend/*",
      "use": "@vercel/static-build",
      "config": { "outputDirectory": "public" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
